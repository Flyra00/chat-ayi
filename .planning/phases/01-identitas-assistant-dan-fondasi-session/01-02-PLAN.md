---
phase: 01-identitas-assistant-dan-fondasi-session
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ChatAyi/Services/PromptContextAssembler.cs
  - ChatAyi/Pages/ChatPage.xaml.cs
  - ChatAyi/MauiProgram.cs
autonomous: true
requirements:
  - PERS-01
  - PERS-02
  - SESS-02
  - PRIV-01
must_haves:
  truths:
    - Semua request chat melewati satu perakitan context terpusat dengan prioritas blok yang terkunci.
    - Persona dan profile memengaruhi format respons secara konsisten di semua jalur chat.
    - Resume session lama tetap membawa context relevan (recent turns + summary) tanpa memblokir chat saat summary tidak tersedia.
  artifacts:
    - path: ChatAyi/Services/PromptContextAssembler.cs
      provides: Build prompt context dengan urutan System > Persona > UserProfile > SessionContext > UserMessage
    - path: ChatAyi/Pages/ChatPage.xaml.cs
      provides: Integrasi assembler pada jalur chat normal dan command path
    - path: ChatAyi/MauiProgram.cs
      provides: Registrasi DI untuk assembler dan store pendukung
  key_links:
    - from: ChatAyi/Pages/ChatPage.xaml.cs
      to: ChatAyi/Services/PromptContextAssembler.cs
      via: Build request messages sebelum StreamChatAsync
      pattern: Build\(
    - from: ChatAyi/Services/PromptContextAssembler.cs
      to: ChatAyi/Models/SessionContextSnapshot.cs
      via: Inject recent turns (6) + optional summary (<=5 bullet)
      pattern: SessionContextSnapshot
    - from: ChatAyi/Services/SessionCatalogStore.cs
      to: ChatAyi/Pages/ChatPage.xaml.cs
      via: Load snapshot resume sebelum model call
      pattern: ReadRecentChatAsync
---

<objective>
Memusatkan perakitan prompt context dan menghubungkan persona/profile/session snapshot ke seluruh alur pemanggilan model.

Purpose: Menutup gap konsistensi instruksi lintas command dan memastikan keputusan prioritas context benar-benar enforced di satu titik.
Output: Service assembler terpusat + wiring ChatPage ke assembler untuk chat normal, `/search`, `/browse`, dan resume flow.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identitas-assistant-dan-fondasi-session/01-CONTEXT.md
@.planning/phases/01-identitas-assistant-dan-fondasi-session/01-RESEARCH.md
@.planning/phases/01-identitas-assistant-dan-fondasi-session/01-01-SUMMARY.md
@ChatAyi/Pages/ChatPage.xaml.cs
</context>

<tasks>

<task type="auto">
  <name>task 1: implementasikan PromptContextAssembler terpusat sesuai urutan blok terkunci</name>
  <files>ChatAyi/Services/PromptContextAssembler.cs</files>
  <action>Buat service assembler yang menerima input safety block, persona, user profile, session snapshot, dan user message, lalu menyusun message list dengan urutan wajib: `System safety + app boundaries` -> `Persona` -> `User profile` -> `Session context` -> `User message`. Terapkan rule resume context: recent turns maksimal 6, summary maksimal 5 bullet, dan jika summary kosong/gagal maka fallback ke recent turns saja (jangan throw). Gunakan mapping profile hanya untuk language/response-length/formality supaya tidak mengubah inti reasoning.</action>
  <verify><automated>dotnet build ChatAyi/ChatAyi.csproj -nologo</automated></verify>
  <done>Assembler bisa dipanggil dari satu entrypoint, selalu menghasilkan urutan blok benar, dan menjaga fallback session context tanpa memblokir request.</done>
</task>

<task type="auto">
  <name>task 2: wire ChatPage ke assembler untuk semua jalur request</name>
  <files>ChatAyi/Pages/ChatPage.xaml.cs, ChatAyi/MauiProgram.cs</files>
  <action>Refactor `ChatPage` agar tidak lagi merakit prompt style inline per branch; semua jalur (`normal`, `/search`, `/browse`) wajib memakai `PromptContextAssembler`. Integrasikan load profile/persona global dan `SessionContextSnapshot` dari store yang sudah ada tanpa mengubah kontrak/struktur store di wave ini; pastikan konflik context memprioritaskan recent turns. Registrasikan dependency baru di `MauiProgram` dengan pola DI yang sudah ada. Tetap local-first: hanya kirim field persona/profile yang diperlukan ke provider (gunakan mapping eksplisit, jangan kirim metadata internal/session index mentah).</action>
  <verify><automated>dotnet build ChatAyi/ChatAyi.csproj -nologo</automated></verify>
  <done>Semua request model menggunakan assembler yang sama, persona/profile konsisten lintas command, dan payload ke provider hanya membawa context minimal yang dibutuhkan.</done>
</task>

</tasks>

<verification>
Jalankan `dotnet build ChatAyi/ChatAyi.csproj -nologo` lalu verifikasi cepat via code inspection bahwa tidak ada lagi prompt instruction style utama yang tersebar di branch command tanpa melewati assembler.
</verification>

<success_criteria>
Perakitan context menjadi satu pintu dengan urutan prioritas terkunci; persona/profile berpengaruh konsisten lintas alur; resume context memakai snapshot recent+summary sesuai kebijakan Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identitas-assistant-dan-fondasi-session/01-02-SUMMARY.md`
</output>
