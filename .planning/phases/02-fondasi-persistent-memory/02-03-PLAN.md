---
phase: 02-fondasi-persistent-memory
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - ChatAyi/Pages/ChatPage.xaml.cs
autonomous: true
requirements:
  - MEM-02
  - MEM-03
must_haves:
  truths:
    - User punya kontrol eksplisit untuk melihat, mengubah, dan menghapus memory item.
    - Penyimpanan memory hanya terjadi saat explicit trigger (command formal atau intent natural simpan).
    - User bisa mematikan memory sementara untuk sesi chat aktif tanpa menghapus memory tersimpan.
  artifacts:
    - path: ChatAyi/Pages/ChatPage.xaml.cs
      provides: Handler command memory explicit (`add/list/update/delete/on/off`) dan intent natural save dengan toggle temporary memory pada sesi aktif.
  key_links:
    - from: ChatAyi/Pages/ChatPage.xaml.cs
      to: ChatAyi/Services/PersonalMemoryStore.cs
      via: Command CRUD memory untuk operasi explicit user.
      pattern: ListAsync|AddAsync|UpdateAsync|DeleteAsync
    - from: ChatAyi/Pages/ChatPage.xaml.cs
      to: ChatAyi/Services/PromptContextAssembler.cs
      via: Bypass retrieval/injection saat mode memory temporary off.
      pattern: Build\(
---

<objective>
Menyelesaikan kontrol user atas lifecycle memory dan mode temporary tanpa mengorbankan keputusan explicit-only di phase ini.

Purpose: Menutup MEM-02 dan MEM-03 dengan kontrol nyata di runtime chat, termasuk disable memory sementara yang reversible untuk sesi aktif.
Output: Command memory eksplisit, intent natural save terkontrol, dan toggle memory sementara untuk sesi aktif (tanpa perubahan metadata session).
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fondasi-persistent-memory/02-CONTEXT.md
@.planning/phases/02-fondasi-persistent-memory/02-RESEARCH.md
@.planning/phases/01-identitas-assistant-dan-fondasi-session/01-03-SUMMARY.md
@.planning/phases/02-fondasi-persistent-memory/02-02-SUMMARY.md
@ChatAyi/Pages/ChatPage.xaml.cs
</context>

<tasks>

<task type="auto">
  <name>task 1: implementasikan explicit controls memory untuk list, edit, delete, dan save</name>
  <files>ChatAyi/Pages/ChatPage.xaml.cs</files>
  <action>Tambahkan command formal `/memory add`, `/memory list`, `/memory update`, `/memory delete` (alias yang konsisten boleh ditambah selama tetap explicit). Gunakan `PersonalMemoryStore` sebagai satu-satunya sumber CRUD dan tampilkan hasil ringkas ke bubble assistant agar user bisa audit perubahan. Tambahkan deteksi intent natural save hanya untuk frase explicit seperti "ingat ini" atau "tolong simpan ini"; intent ini harus memicu flow save eksplisit (bukan auto-capture diam-diam) dan tetap memetakan kategori ke tiga kategori locked decision.</action>
  <verify><automated>dotnet build ChatAyi/ChatAyi.csproj -nologo</automated></verify>
  <done>User dapat melakukan view/edit/delete memory lewat kontrol eksplisit, serta save via command formal atau natural explicit intent.</done>
</task>

<task type="auto">
  <name>task 2: tambahkan mode memory temporary-off untuk sesi aktif tanpa delete data</name>
  <files>ChatAyi/Pages/ChatPage.xaml.cs</files>
  <action>Tambahkan toggle minimal `/memory off` dan `/memory on` untuk sesi chat aktif saja (runtime flag pada `ChatPage`, tidak perlu persist ke catalog/metadata session). Saat mode off, bypass retrieval dan injeksi memory pada request chat normal, tetapi operasi CRUD memory tetap tersedia dan data yang sudah tersimpan tidak dihapus. Saat sesi baru dimulai atau halaman chat dibuka ulang, default kembali `on` untuk menjaga kompleksitas tetap rendah.</action>
  <verify><automated>dotnet build ChatAyi/ChatAyi.csproj -nologo</automated></verify>
  <done>Mode temporary memory bekerja untuk sesi aktif, reversible, dan tidak menghapus memory existing (memenuhi MEM-03 dengan implementasi minimum).</done>
</task>

</tasks>

<verification>
 Jalankan `dotnet build ChatAyi/ChatAyi.csproj -nologo`, lalu uji cepat command sequence: `/memory add ...` -> `/memory list` -> `/memory off` -> kirim chat normal (tanpa memory injection) -> `/memory on` -> `/memory list` tetap menampilkan item yang sama.
</verification>

<success_criteria>
User memiliki kontrol eksplisit minimum lifecycle memory (view/edit/delete/save) dan bisa menonaktifkan memory sementara untuk sesi aktif tanpa kehilangan data tersimpan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fondasi-persistent-memory/02-03-SUMMARY.md`
</output>
