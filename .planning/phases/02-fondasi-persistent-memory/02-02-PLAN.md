---
phase: 02-fondasi-persistent-memory
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - ChatAyi/Services/PromptContextAssembler.cs
  - ChatAyi/Pages/ChatPage.xaml.cs
autonomous: true
requirements:
  - MEM-01
must_haves:
  truths:
    - Memory relevan muncul di context respons chat berikutnya ketika query berhubungan.
    - Urutan context tetap System > Persona > User Profile > Memory > Session Context > User Message.
    - Jika tidak ada memory relevan, prompt tidak menyertakan memory block kosong.
  artifacts:
    - path: ChatAyi/Services/PromptContextAssembler.cs
      provides: Memory block builder kondisional dengan instruksi conflict handling.
    - path: ChatAyi/Pages/ChatPage.xaml.cs
      provides: Wiring retrieval memory store ke alur chat sebelum request model dikirim.
  key_links:
    - from: ChatAyi/Pages/ChatPage.xaml.cs
      to: ChatAyi/Services/PersonalMemoryStore.cs
      via: Pemanggilan retrieval relevansi berdasarkan prompt user.
      pattern: GetRelevantAsync
    - from: ChatAyi/Pages/ChatPage.xaml.cs
      to: ChatAyi/Services/PromptContextAssembler.cs
      via: BuildInput menyertakan memory relevan sebelum session context.
      pattern: PromptContextAssembler\.BuildInput
    - from: ChatAyi/Services/PromptContextAssembler.cs
      to: model request payload
      via: Pengurutan blok system yang deterministic.
      pattern: role = "system"
---

<objective>
Menghubungkan memory personal ke pipeline prompt supaya memory durable benar-benar dipakai ulang pada percakapan selanjutnya secara terkendali.

Purpose: Memenuhi outcome MEM-01 di level runtime dengan injeksi context yang relevan, ringkas, dan tidak mengganggu prioritas persona/session.
Output: Prompt assembler dengan memory layer baru dan ChatPage wiring retrieval-memory untuk alur chat utama.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fondasi-persistent-memory/02-CONTEXT.md
@.planning/phases/02-fondasi-persistent-memory/02-RESEARCH.md
@.planning/phases/01-identitas-assistant-dan-fondasi-session/01-02-SUMMARY.md
@.planning/phases/02-fondasi-persistent-memory/02-01-SUMMARY.md
@ChatAyi/Services/PromptContextAssembler.cs
@ChatAyi/Pages/ChatPage.xaml.cs
</context>

<tasks>

<task type="auto">
  <name>task 1: tambah memory layer di PromptContextAssembler sesuai urutan terkunci</name>
  <files>ChatAyi/Services/PromptContextAssembler.cs</files>
  <action>Perluas `BuildInput` agar menerima daftar memory relevan terstruktur (id, category, content). Sisipkan memory block tepat setelah profile block dan sebelum session block. Format memory block wajib bullet ringkas dengan tag kategori per item. Tambahkan instruksi eksplisit: memory hanya referensi, dipakai jika relevan, dan jika bertentangan dengan pesan user terbaru maka pesan user terbaru menang. Jika daftar memory kosong, jangan inject memory block sama sekali.</action>
  <verify><automated>dotnet build ChatAyi/ChatAyi.csproj -nologo</automated></verify>
  <done>Assembler menghasilkan payload dengan urutan context terkunci dan memory block hanya muncul saat ada item relevan.</done>
</task>

<task type="auto">
  <name>task 2: wire retrieval memory terstruktur ke alur request chat</name>
  <files>ChatAyi/Pages/ChatPage.xaml.cs</files>
  <action>Ganti pemakaian context memory lama berbasis chunk dengan retrieval dari `PersonalMemoryStore` untuk alur chat utama (`OnSendClicked` non-command). Ambil maksimal 5 item relevan dari query user, kirim ke `PromptContextAssembler.Build`, dan pertahankan fallback aman jika retrieval gagal (chat tetap jalan tanpa memory block). Jangan menambah implicit memory capture pada alur normal; penyimpanan memory tetap explicit-only sesuai keputusan user.</action>
  <verify><automated>dotnet build ChatAyi/ChatAyi.csproj -nologo</automated></verify>
  <done>Percakapan normal memakai memory personal relevan saat tersedia, namun tetap bersih tanpa memory block ketika tidak relevan.</done>
</task>

</tasks>

<verification>
Jalankan `dotnet build ChatAyi/ChatAyi.csproj -nologo` dan pastikan payload request yang dibentuk assembler tidak menghasilkan memory block kosong ketika query tidak cocok dengan memory apapun.
</verification>

<success_criteria>
Memory personal durable benar-benar direuse di runtime chat, urutan context tetap sesuai locked decision, dan noise prompt turun karena injeksi hanya saat relevan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fondasi-persistent-memory/02-02-SUMMARY.md`
</output>
